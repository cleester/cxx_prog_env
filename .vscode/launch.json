{
    "configurations": [
    {
        "name": "(lldb) Framework",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/build/app",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}
